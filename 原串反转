#include<stdio.h>

int main()
{
    char a[3000];
    int i=0,len,j=0;
    gets(a);
    len=strlen(a);
    char b[len];
    for(i=len-1;i>=0;i--)
    {
       b[j]=a[i];
       j++;
    }
    puts(b);
    return 0;
}
